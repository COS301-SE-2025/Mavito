# Application Settings
PROJECT_NAME="Mavito API - Local Dev Zainab"
SECRET_KEY="p44d6c8350cd925eb33437e07deeab6e847633b1c4cc1260cd4cc5a15c37cad8f"
ACCESS_TOKEN_EXPIRE_MINUTES=10080
# --- PostgreSQL Connection Details (Choose ONE setup for local development) ---
# Option A: Local PostgreSQL using Docker (Recommended for isolated local development)
# If using this, make sure your Docker PostgreSQL container is running (see Phase 2, Step 2a)
# and these credentials match your Docker container's setup.
DB_USER="mavito_dev_user"
DB_PASSWORD="mavito_dev_password"
DB_NAME="mavito_local_dev_db"
DB_HOST="127.0.0.1" # Or localhost
DB_PORT="5432"
INSTANCE_CONNECTION_NAME="" # Leave BLANK or COMMENT OUT if using local Docker PG / TCP Proxy
# Option B: Connecting to a Shared Development Cloud SQL Instance via Cloud SQL Proxy (TCP Mode)
# If using this, get the actual values from your team lead who set up Cloud SQL.
# Ensure Cloud SQL Auth Proxy is running (see Phase 2, Step 2b).
# DB_USER="your_shared_cloudsql_user"
# DB_PASSWORD="your_shared_cloudsql_password"
# DB_NAME="your_shared_cloudsql_database_name"
# DB_HOST="127.0.0.1" # Proxy listens here
# DB_PORT="5432" # Proxy listens here
# INSTANCE_CONNECTION_NAME="" # Leave BLANK as cong.py will use HOST/PORT for TCP
# --- CORS Seings ---
# Your GitHub Organization name for GitHub Pages CORS origin.
GITHUB_ORGANIZATION="cos301-se-2025" # Use lowercase
# Add your local frontend development URL(s) here, comma-separated.
# Example:
BACKEND_CORS_ORIGINS=hp://localhost:3000,hp://localhost:5173
# If your local frontend runs on port 3000 (common for Create React App):
BACKEND_CORS_ORIGINS="hp://localhost:3000"
# If your local frontend runs on port 5173 (common for Vite):
# BACKEND_CORS_ORIGINS="hp://localhost:5173"
# If you use both or others, list them comma-separated